from pathlib import Path

resources_dir = (Path.cwd() / ".." / ".." / "resources").resolve()
fonts_dir = resources_dir / "fonts"
graphics_dir = resources_dir / "graphics"
music_dir = resources_dir / "music"
sound_dir = resources_dir / "sound"
